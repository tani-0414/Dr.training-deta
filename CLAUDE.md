# Dr.training

パーソナルジムチェーン向け 業務効率化・データ活用プロジェクト群。

---

## プロジェクト構成

| フォルダ | 内容 | 状態 |
|--------|------|------|
| `data-analysis/` | 取引データ・会員データの集計・分析 | 稼働中 |
| `dashboard/` | 実績ダッシュボード（Streamlit Web アプリ） | 稼働中 |

---

## 共通ルール

### 個人情報の取り扱い（全プロジェクト必須）

顧客データを扱う際は、以下のルールを必ず守ること。

**表示・利用してよい項目**
- 名前・年齢・性別

**必ずマスク・非表示にする項目**
- 電話番号 → `090-****-****`
- メールアドレス → `***@***.***`
- 住所 → `東京都***`（都道府県のみ）
- 生年月日 → 年齢に変換
- クレジットカード番号 → `****-****-****-1234`（末尾4桁のみ）
- マイナンバー・パスポート番号などID類 → 完全非表示

**実装方針**
- データはシステム内部でもマスク済みの状態で扱う
- ログにも生データを出力しない
- 外部APIへの送信時も必ずマスク処理を施してから送る

---

## 新しいプロジェクトの追加方法

```
新しいプロジェクトを追加して：[プロジェクト名]
内容：[やりたいこと・目的]
```
